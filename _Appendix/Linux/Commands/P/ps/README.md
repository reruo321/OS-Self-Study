# `ps`
`ps` is a command to view the information of processes, whose name is the abbreviation of **Process Status**.
It reads the information from the virtual files in a virtual file system, `/proc`.
